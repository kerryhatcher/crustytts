[package]
name = "crustytts-sink"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Audio output sinks for crustytts: aplay (ALSA) and in-memory capture for testing."
keywords = ["tts", "audio", "aplay"]
categories = ["multimedia::audio"]
repository = "https://github.com/kerryhatcher/crustytts"

[dependencies]
crustytts-core = { version = "0.1.0", path = "../crustytts-core" }
